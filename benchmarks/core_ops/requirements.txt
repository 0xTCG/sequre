# CrypTen 0.4.1 (2022) is installed separately with --no-deps; these are its
# real runtime dependencies plus MPyC's.
#
# torch is pinned below 2.5 because crypten/nn/onnx_converter.py imports
# torch.onnx._internal.registration, which newer torch no longer has. numpy is
# pinned below 2 for the same vintage reason.
torch==2.4.1
numpy<2
onnx==1.16.2
omegaconf
pyyaml
pandas
scipy
scikit-learn
tensorboard

# MPyC. gmpy2 is optional but MPyC uses it for a large speedup in its field
# arithmetic, so it is installed to benchmark MPyC at its best.
mpyc==0.11
gmpy2
