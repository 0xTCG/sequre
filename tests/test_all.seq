from sequre.mpc.env import MPCEnv
from sequre.utils.param import FIELD_PRIME, RING_SIZE


def test_all[TP](mpc: MPCEnv[TP]):
    from unit_tests import test_unit
    test_unit(mpc)

    from e2e_tests import test_e2e
    test_e2e(mpc, FIELD_PRIME)
    # mpc.comms.sync_parties()
    # test_e2e(mpc, RING_SIZE)

    print(f'All tests passed at {mpc.pid}!\n')
