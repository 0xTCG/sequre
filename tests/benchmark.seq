from sequre.stdlib.lin_alg import *
from sequre.stdlib.fp import *
from ..protocols.seq_gwas import gwas_protocol as seq_gwas
from ..protocols.sequre_gwas import gwas_protocol as sequre_gwas
from ..protocols.seq_logireg import logireg_protocol as seq_logireg
from ..protocols.sequre_logireg import logireg_protocol as sequre_logireg
from ..protocols.seq_nn import neural_net_protocol as seq_nn
from ..protocols.sequre_nn import neural_net_protocol as sequre_nn
from ..playground import playground

from sequre.utils.param import *
from sequre.utils.utils import get_address, evalp
from sequre.types.utils import double_to_fp

from sequre.mpc.env import MPCEnv

from C import fork() -> int
from C import wait(cobj) -> int
from C import unlink(str) -> int

from sequre.attributes import *


def benchmark(mpc):
    # a = SecureContainer[list[list[int_t]]](
    #     [[int_t(1), int_t(1)],
    #     [int_t(1), int_t(1)]])
    # b = SecureContainer[list[list[int_t]]](
    #     [[int_t(2), int_t(2)],
    #     [int_t(2), int_t(2)]])
    # c = SecureContainer[list[list[int_t]]](
    #     [[int_t(3), int_t(3)],
    #     [int_t(3), int_t(3)]])
    a = SecureContainer[list[int_t]]([int_t(1), int_t(1)])
    b = SecureContainer[list[int_t]]([int_t(2), int_t(2)])
    c = SecureContainer[list[int_t]]([int_t(3), int_t(3)])
    # a = SecureContainer[int_t](1)
    # b = SecureContainer[int_t](2)
    # c = SecureContainer[int_t](3)

    vec = [
        double_to_fp(1.5, NBIT_K, NBIT_F),
        double_to_fp(0.5, NBIT_K, NBIT_F),
        double_to_fp(2.5, NBIT_K, NBIT_F)]
    sv = SecureContainer[list[int_t]](vec)
    sv.fp = True
    
    h_vec = [double_to_fp(float(i), NBIT_K, NBIT_F) for i in range(500000)]
    sv_vec = SecureContainer[list[int_t]](h_vec)
    sv_vec.fp = True

    h_mat = [[double_to_fp(float(i + j), NBIT_K, NBIT_F) for i in range(50)] for j in range(50)]
    sv_mat = SecureContainer[list[list[int_t]]](h_mat)
    sv_mat.set_partitions(mpc.arithmetic.__beaver_partition(sv_mat.share, BASE_P))
    sv_mat.fp = True

    import time
    s = time.time()
    mpc.reset_stats()

    # Playground samples #
    ######################
    print('Final result: ', mpc.comms.reveal(playground(mpc, a, b, c).share))
    
    # ####### Seq FP #######
    # ######################
    # result = mpc.fp.fp_sqrt(h_vec[:1000])

    # ###### Sequre FP #####
    # ######################
    # result = fp_sqrt(mpc, sv_vec[:1000])

    # #### Seq lin alg #####
    # ######################
    # result = mpc.lin_alg.householder(h_vec)
    # result_0 = mpc.lin_alg.qr_fact_square(h_mat)
    # result_1 = mpc.lin_alg.tridiag(h_mat)
    # result_2 = mpc.lin_alg.eigen_decomp(h_mat)
    # result_3 = mpc.lin_alg.orthonormal_basis(h_mat)

    # ### Sequre lin alg ###
    # ######################
    # result = householder(mpc, sv_vec)
    # result_0 = qr_fact_square(mpc, sv_mat)
    # result_1 = tridiag(mpc, sv_mat)
    # result_2 = eigen_decomp(mpc, sv_mat, ITER_PER_EVAL)
    # result_3 = orthonormal_basis(mpc, sv_mat)

    # ###### Seq GWAS ######
    # ######################
    # seq_gwas(mpc, True)

    # #### Sequre GWAS #####
    # ######################
    # sequre_gwas(mpc, True)

    # #### Seq logireg #####
    # ######################
    # seq_logireg(mpc, True)

    # ### Sequre logireg ###
    # ######################
    # sequre_logireg(mpc, True)

    # #### Seq neural net #####
    # ######################
    # seq_nn(mpc, True)

    # ### Sequre neural net ###
    # ######################
    # sequre_nn(mpc, True)

    e = time.time()
    print(f'Done in {e - s}s at {mpc.pid}')
    mpc.print_stats()

    mpc.comms.sync_parties()
