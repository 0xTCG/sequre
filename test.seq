from time import time

s = time()
print(3 * 2)
e = time()

print(e - s)


def beaver_triplets_mult(mpc, x, y):
    if mpc.pid == 0:  # If computing as a trusted dealer
        a = randint()
        b = randint()
        c = a * b
        secret_share(a, b, c)
    else:  # If computing as a computing party
        a, b, c = receive_secrets()

        x_a = reveal(x - a)
        y_b = reveal(y - b)

        return x_a * y_b + x_a * b + y_b * a + c

print("Calling Beaver triplets routine")

@sequre
def foo(mpc):
    a, b = random_share(), random_share()
    return a * b

foo(mpc)