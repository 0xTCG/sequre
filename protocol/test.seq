from ..mpc.env import MPCEnv
from ..tests.test_all import test_all, benchmark


def test_protocol[TP](mpc: MPCEnv[TP], test_run: bool) -> bool:
    if test_run:
        benchmark[TP](mpc, m=50, n=50)
    else:
        test_all[TP](mpc)
    return True
