from mpc_env import MPCEnv
from tests import test_all, benchmark


def test_protocol[TP](mpc: MPCEnv[TP], pid: int, test_run: bool = True) -> bool:
    test_all[TP](mpc, pid) if test_run else benchmark[TP](mpc, pid, m=50, n=50)
    return True
