from mpc_env import MPCEnv
from tests import test_all, benchmark


def test_protocol[TP](mpc: MPCEnv[TP], pid: int, test_run: bool) -> bool:
    if test_run:
        test_all[TP](mpc, pid)
    else:
        benchmark[TP](mpc, pid, m=50, n=50)
    return True
