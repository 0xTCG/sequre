from mpc_env import MPCEnv
from tests import test_all, benchmark


def test_protocol[TP](mpc: MPCEnv[TP], pid: int) -> bool:
    test_all[TP](mpc, pid)
    # benchmark[TP](mpc, pid, m=5, n=5)
    return True
