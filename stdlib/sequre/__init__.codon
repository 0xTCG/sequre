from attributes import *
from types.builtin import *

from utils.data_sharing import *
from utils.utils import zeros, ones
def zeros_like(val): return val.zeros()

from stdlib.internal import Internal as sq
def dot(mpc, a, b): return sq.dot(mpc, a, b)

from settings import DEBUG
from utils.param import FIELD_SIZE, RING_SIZE

if DEBUG:
    print(f"Sequre setup:\n\t Galois field size: {FIELD_SIZE.__str__()}\n\t Z_(2^k) ring size: {RING_SIZE.__str__()}")
