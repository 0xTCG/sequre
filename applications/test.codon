from sequre.mpc.env import MPCEnv
from ..tests.tests import tests
from ..tests.benchmark import benchmark


def tests_protocol[TP](mpc: MPCEnv[TP], test_toggles: dict, modulus: TP):
    tests(mpc, test_toggles)


def benchmarks_protocol[TP](mpc: MPCEnv[TP], benchmark_toggles: dict, modulus: TP):
    benchmark(mpc, benchmark_toggles, modulus)
